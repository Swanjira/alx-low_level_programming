#include "main.h"
#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: 0 on success
 */

int main(void)
{
	char *str = "\"Programming is like building a multilingual puzzle";

	puts(str);
	return (0);
}
