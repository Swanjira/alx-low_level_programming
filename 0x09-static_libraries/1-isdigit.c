#include "main.h"

/**
 * _isdigit - checks for dihits 1 through 9
 * @c: integer to be checked
 *
 * Return: 1 if c is int 0 if c is not int
 */

int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
